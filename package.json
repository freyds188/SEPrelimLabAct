{
  "name": "selabactprelim",
  "version": "1.0.0",
  "description": "Traditional Filipino Weaving Platform - Next.js Frontend + Laravel Backend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd api && php artisan serve --host=127.0.0.1 --port=8000",
    "frontend:build": "cd frontend && npm run build",
    "frontend:start": "cd frontend && npm start",
    "backend:migrate": "cd api && php artisan migrate",
    "backend:migrate:fresh": "cd api && php artisan migrate:fresh",
    "backend:seed": "cd api && php artisan db:seed",
    "backend:cache:clear": "cd api && php artisan config:clear && php artisan cache:clear",
    "install:all": "npm install && cd frontend && npm install && cd ../api && composer install",
    "setup": "npm run install:all && npm run backend:migrate && npm run backend:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "nextjs",
    "laravel",
    "filipino",
    "weaving",
    "traditional",
    "artisan"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}



